<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.org.kopernik.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="8" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".KopernikApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".KopernikActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExhibitActivity" />
        <activity android:name=".ExhibitDetailsActivity" />
        <activity android:name=".PathActivity" />
        <activity android:name=".PathBeginActivity" />
        <activity android:name=".PathSummaryActivity" />
        <activity android:name=".FacebookIntegrationActivity" />
    </application>

</manifest>